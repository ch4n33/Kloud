[defaults]
inventory = inventory.yml
remote_user = pi
host_key_checking = False
timeout = 30
forks = 4

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
